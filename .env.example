# Local dev (optional — defaults to http://localhost:8000/api/v1)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Vercel production (optional — defaults to DigitalOcean API when NODE_ENV=production)

# App URL — must match BE CORS_ORIGIN (trailing slash is OK)

